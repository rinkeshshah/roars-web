node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.*
!.env.example

# Never commit a filled form config. The real one lives outside the docroot.
public/api/contact-config.php
exports/
.compare/

# Never commit a filled secrets file. The real one lives outside the docroot.
public/api/roars-secrets.php

# Generated by scripts/build-work-responsive.mjs, which runs in `prebuild`.
# 152 files and about 10 MB of AVIF and WebP derived from public/work/*.jpg.
# Any build makes them; committing them would put ten megabytes of generated
# binary in the history to save a step that already runs automatically.
public/work/r/

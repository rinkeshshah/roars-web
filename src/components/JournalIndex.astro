---
/**
 * /our-journal/ — the writing.
 *
 * Built against design/prototypes/Roars v2 - Insights.dc.html.
 * Canvas, section-relative: Header 780, Insights 1620.
 *
 * DRIVEN BY THE COLLECTION. It used to be driven by the inventory, because
 * src/content/posts/ was empty and an index that waits for a migration is an
 * index that links nothing. The export has landed, so the cards are posts
 * again: real titles, real publish dates, real heroes, real excerpts.
 *
 * The unmigrated URLs are no longer listed. They still build and still return
 * 200 — [slug].astro takes its paths from the inventory — but a list of
 * ninety cards that mostly open on "content pending migration" is worse than
 * a shorter list that is all readable. See src/lib/journal.ts.
 *
 * The hero card is back too, for the same reason: there is now a newest post
 * with an image, rather than whatever happened to sort first.
 */
import BaseLayout from '../layouts/BaseLayout.astro'
import Section from './Section.astro'
import TopBar from './TopBar.astro'
import NavOverlay from './NavOverlay.astro'
import SiteFooter from './SiteFooter.astro'
import GrainField from './GrainField.astro'
import { PORTRAIT, JOURNAL_IMG } from '../lib/media'
import { site } from '../lib/site'
import star5 from '../assets/star-5.svg'

import type { JournalItem } from '../lib/journal'
interface Props {
  posts: JournalItem[]
  /** The lead cards, newest first. Page one only; page two onward opens
   *  straight on the grid. One is drawn static, two or more cross-fade. */
  featured?: JournalItem[]
  page: number
  lastPage: number
  total: number
}
const { posts, featured, page, lastPage, total } = Astro.props

const path = page === 1 ? '/our-journal/' : `/our-journal/page/${page}/`
const pageUrl = (n: number) => (n === 1 ? '/our-journal/' : `/our-journal/page/${n}/`)

/* Every page number, because there are two of them.
 *
 * This used to window to five around the current page with an ellipsis for
 * the gap, which was right when the index listed all 115 inventory rows. The
 * index now lists migrated posts only, so the pager is two numbers wide and
 * the elision could never fire — leaving a styled ellipsis that nothing
 * rendered, which assert-styles correctly refused.
 *
 * Bring both back together, not separately, once the journal passes about
 * nine pages and a single row starts to wrap. */
const window = Array.from({ length: lastPage }, (_, i) => i + 1)

const schema = [
  {
    '@context': 'https://schema.org',
    '@type': 'CollectionPage',
    '@id': `${site.url}${path}#collection`,
    name: 'Insights',
    isPartOf: { '@id': `${site.url}/#website` },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Home', item: `${site.url}/` },
      { '@type': 'ListItem', position: 2, name: 'Insights' },
    ],
  },
]
---

<BaseLayout
  title="Insights: Product Work, Process and Nuance | Roars"
  description="We have a lot to say about product work, how we approach the process, and the nuances of building for founders. Twenty years of it, written down."
  path={path}
  schema={schema}
  noindex={page > 1 ? 'follow' : false}
>
  <TopBar slot="topbar" cta="contact" />
  <NavOverlay />

  {/* ── Header, 1440x780 ───────────────────────────────────── */}
  <Section ground="dark" bleed>
    <div class="jh">
      <GrainField tone="dark" second />
      <div class="wrap jh__inner">
        <div class="jh__rail" data-reveal>
          <span class="jh__who">
            <img src={PORTRAIT.founder} alt="" data-decorative width="42" height="42" />
            <span class="jh__name">Rinkesh A Shah</span>
            <span class="jh__role">Founder</span>
          </span>
          <p class="jh__statement">
            We have a lot to say about product work, how we approach the process, and the
            nuances of building for founders.
          </p>
        </div>

        <div class="jh__mast" data-reveal>
          <span class="jh__chip">
            <span>roars</span>
            <span class="starmark jh__star" style={`--m:url(${star5.src})`} aria-hidden="true"></span>
          </span>
          <h1 class="jh__large">Insights</h1>
        </div>

        <div class="jh__count" data-reveal>
          <span class="jh__disc">{total}</span>
          <span class="jh__count-label">Pieces written</span>
        </div>
      </div>
    </div>
  </Section>

  {/* ── The list, three across ─────────────────────────────── */}
  <Section ground="light">
    <div class="jl">
      <span class="jl__rule" aria-hidden="true"></span>
      <span class="jl__eyebrow">
        {page === 1 ? `ALL INSIGHTS / ${total}` : `PAGE ${page} OF ${lastPage} / ${total} PIECES`}
      </span>

      {featured && featured.length > 0 && (
        /* The lead slot. Two posts stacked and cross-faded; the pips top-right
           are the export's two dots, doing a job now instead of decorating.
           NO TITLE OVER THE ART. The hero images carry their own headline
           panel, so printing the post name on top of it set the title twice,
           once in each typeface. The date stays, bottom-left, away from the
           panel the artwork puts on the right. */
        <div class="jf" data-reveal data-jf={featured.length > 1 ? '' : undefined}>
          {featured.map((f, i) => (
            <a
              class:list={['jf__slide', i === 0 && 'is-on']}
              href={f.href}
              data-jf-slide
              aria-hidden={i > 0 ? 'true' : undefined}
              tabindex={i > 0 ? -1 : undefined}
            >
              <span class="jf__shot">
                {(f.hero ?? JOURNAL_IMG[f.slug]) && (
                  <img
                    src={f.hero ?? JOURNAL_IMG[f.slug]}
                    alt="" data-decorative
                    width="1000"
                    height="425"
                    loading={i === 0 ? 'eager' : 'lazy'}
                    decoding="async"
                  />
                )}
              </span>
              <span class="jf__cap">
                <span class="jf__date">
                  <b>{f.day}</b><i>{f.month}</i>
                </span>
              </span>
              {/* The date is the only thing the caption prints, which left the
                  anchor reading "04 May" to a crawler and to a screen reader.
                  The title goes in unpainted rather than on the art, so the
                  link says what it links to without setting the headline
                  twice. */}
              <span class="visually-hidden">{f.name}</span>
            </a>
          ))}
          {featured.length > 1 && (
            <span class="jf__pips" aria-hidden="true">
              {featured.map((_, i) => (
                <span class:list={['jf__dot', i === 0 && 'is-on']} data-jf-pip></span>
              ))}
            </span>
          )}
        </div>
      )}

      <ul class="jl__grid">
        {posts.map((p, i) => (
          <li data-reveal style={`transition-delay:${(i % 3) * 60}ms`}>
            <a href={p.href}>
              {/* The export's circular thumbnail. Every migrated post brought
                  its WordPress hero across, so this is the post's own image.
                  The guard stays for the day one arrives without one: a circle
                  of flat colour is honest where a stand-in photo would not
                  be. */}
              <span class="jl__disc" aria-hidden="true">
                {(p.hero ?? JOURNAL_IMG[p.slug]) && (
                  <img src={p.hero ?? JOURNAL_IMG[p.slug]} alt="" data-decorative width="138" height="138" loading="lazy" decoding="async" />
                )}
              </span>
              <span class="jl__hair" aria-hidden="true"></span>
              {p.day && (
                <span class="jl__date"><b>{p.day}</b><i>{p.month}</i></span>
              )}
              <span class="jl__title">{p.name}</span>
            </a>
          </li>
        ))}
      </ul>

      {lastPage > 1 && (
        <nav class="jl__pager" aria-label="Insights pages">
          <a
            class="jl__step"
            href={page > 1 ? pageUrl(page - 1) : undefined}
            aria-disabled={page === 1 ? 'true' : undefined}
            /* rel only when there is an href for it to describe. On page 1 the
               Previous control has no destination, and `rel` without `href` is
               invalid — it describes a relationship to nothing. */
            rel={page > 1 ? 'prev' : undefined}
          >← Previous</a>

          <span class="jl__nums">
            {window.map((n) => (
              <a
                href={pageUrl(n)}
                aria-current={n === page ? 'page' : undefined}
              >{n}</a>
            ))}
          </span>

          <a
            class="jl__step"
            href={page < lastPage ? pageUrl(page + 1) : undefined}
            aria-disabled={page === lastPage ? 'true' : undefined}
            rel={page < lastPage ? 'next' : undefined}
          >Next →</a>
        </nav>
      )}
    </div>
  </Section>

  <SiteFooter
    slot="footer"
    blurbTitle="Reading is cheaper than learning it twice."
    blurb="Most of what is here we worked out the expensive way. If one of these is the problem in front of you, we can go deeper than the post does."
    tagline="twenty years, written down"
    ctaLabel="Shall we chat?"
  />
</BaseLayout>

<style>
  /* ── header ─────────────────────────────────────────────── */
  .jh { position: relative; }
  .jh__inner {
    position: relative; z-index: 1;
    display: grid; grid-template-columns: 449px minmax(0, 1fr);
    align-content: start; padding-block: 96px 0; min-height: 780px;
  }
  .jh__rail { grid-column: 1; grid-row: 1; width: 384px; }
  .jh__who { position: relative; display: block; height: 42px; }
  .jh__who img { width: 42px; height: 42px; border-radius: 50%; object-fit: cover; background: rgba(255, 255, 255, 0.12); }
  .jh__name, .jh__role {
    position: absolute; left: 56px;
    font-size: var(--fs-label); line-height: 20px; letter-spacing: -0.05em;
  }
  .jh__name { top: 6px; font-weight: var(--fw-semi); color: rgba(255, 255, 255, 0.95); }
  .jh__role { top: 25px; font-weight: var(--fw-medium); color: rgba(255, 255, 255, 0.6); }
  .jh__statement {
    margin: 32px 0 0; width: 384px;
    font-size: var(--fs-intro); line-height: 30px; font-weight: var(--fw-medium);
    letter-spacing: var(--ls-item); color: rgba(255, 255, 255, 0.92);
  }

  .jh__mast { grid-column: 2; grid-row: 1; margin-top: 367px; }
  .jh__chip { position: relative; display: flex; align-items: flex-start; height: 24px; margin-left: 13px; }
  .jh__chip > span:first-child {
    font-size: 20px; line-height: 20px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-item); color: rgb(251, 251, 251); padding-top: 4px;
  }
  .jh__star { position: absolute; left: 52px; top: 0; width: 10px; height: 10px; color: rgb(251, 251, 251); }
  .jh__large {
    margin: 28px 0 0 6px;
    font-size: clamp(56px, 8.62vw, 124px); line-height: 86px; font-weight: var(--fw-semi);
    letter-spacing: -0.04em; color: rgb(251, 251, 251);
  }
  .jh__count {
    grid-column: 2; grid-row: 1; justify-self: end; align-self: start;
    display: flex; align-items: center; gap: 28px; margin-top: 344px;
  }
  .jh__disc {
    display: flex; align-items: center; justify-content: center;
    width: 42px; height: 42px; border-radius: 50%; background: var(--c-white);
    font-size: var(--fs-body); line-height: 24px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-item); color: var(--c-black);
  }
  .jh__count-label {
    width: 127px;
    font-size: var(--fs-label); line-height: 20px; font-weight: var(--fw-medium);
    letter-spacing: var(--ls-item); color: rgba(255, 255, 255, 0.75);
  }

  /* ── the list ───────────────────────────────────────────── */
  .jl { padding-block: 80px 110px; }
  .jl__rule { display: block; height: 1.5px; background: var(--c-rule-strong); }
  .jl__eyebrow {
    display: block; margin-top: 24px;
    font-size: var(--fs-micro); line-height: 19px; font-weight: var(--fw-medium);
    letter-spacing: 0.18em; color: var(--c-ink-label);
  }

  /* ── the featured card ──────────────────────────────────── */
  /* THE BOX IS THE SHAPE OF THE ARTWORK. It was a fixed 536px height on a
     1362px card — 2.54:1 — with `object-fit: cover`. The heroes are all built
     at 1000x425, which is 2.35:1, so cover filled the wider box and cut a
     strip off the top and the bottom of every one. Matching the ratio means
     the image lands exactly, uncropped and unstretched, and the box is still
     a fixed reservation so nothing shifts while it loads. */
  .jf { position: relative; display: block; margin-top: 80px; aspect-ratio: 1000 / 425; }
  /* The slides stack. Only the one showing takes the pointer, so a click can
     never land on the post underneath it. */
  .jf__slide {
    position: absolute; inset: 0; display: block;
    opacity: 0; pointer-events: none;
    transition: opacity 0.7s ease;
  }
  .jf__slide.is-on { opacity: 1; pointer-events: auto; }
  .jf__shot {
    position: absolute; inset: 0; display: block; overflow: hidden;
    border-radius: 12px; background: rgb(30, 30, 30);
  }
  .jf__shot img {
    display: block; width: 100%; height: 100%; object-fit: cover;
    transition: transform var(--dur-slow) ease;
  }
  .jf__slide:hover .jf__shot img { transform: scale(1.03); }
  /* The date is white over the photo, so the photo has to be dark where the
     date is: a bottom-up wash over the lower 46%. */
  .jf__shot::after {
    content: ''; position: absolute; inset: auto 0 0 0; height: 46%;
    background: linear-gradient(0deg, rgba(0, 0, 0, 0.6) 0%, rgba(0, 0, 0, 0) 100%);
  }
  /* The export's two dots, now the rotation indicator. They sit on the card
     rather than inside a slide, so they do not fade with it. */
  .jf__pips { position: absolute; top: 30px; right: 61px; z-index: 2; display: flex; gap: 5px; }
  .jf__dot {
    width: 8px; height: 8px; border-radius: 50%;
    background: rgba(255, 255, 255, 0.55); transition: background var(--dur-fast) ease;
  }
  .jf__dot.is-on { background: var(--c-white); }
  /* Bottom-left, clear of the headline panel the artwork carries on its
     right-hand side. */
  .jf__cap { position: absolute; left: 44px; bottom: 40px; display: block; }
  .jf__date { display: block; width: 88px; }
  .jf__date b {
    font-size: var(--fs-stat); line-height: 43px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-display); color: var(--c-white);
  }
  .jf__date i {
    margin-left: 13px; font-style: normal;
    font-size: 18px; line-height: 43px; font-weight: var(--fw-medium);
    letter-spacing: var(--ls-display); color: var(--c-white);
  }
  /* The export's card: a 138px disc on the right, a rule under it, then the
     date and the title. 435x300, three across. */
  .jl__grid {
    list-style: none; margin: 80px 0 0; padding: 0;
    display: grid; grid-template-columns: repeat(3, 435fr); column-gap: 26px; row-gap: 60px;
  }
  .jl__grid a { position: relative; display: block; min-height: 300px; }
  /* `display: block` IS LOAD-BEARING, not tidiness. This is a <span>, and it
     is only block-level here because it is absolutely positioned. Below the
     design grid the responsive rule returns it to `position: static`, which
     drops it back to inline — and an inline box ignores width and height. The
     88px square went away, the image inside filled the column at its natural
     height, and `border-radius: 50%` turned that rectangle into an ellipse.
     Declaring the display here means the shape no longer depends on how the
     element happens to be positioned. */
  .jl__disc {
    position: absolute; left: 234px; top: 0; display: block;
    width: 138px; height: 138px; border-radius: 50%;
    background: rgb(241, 241, 239);
    transition: background var(--dur-fast) ease;
  }
  .jl__grid a:hover .jl__disc { background: var(--c-accent); }
  /* Same reason as the disc above: a static <span> is inline, and an inline
     box has no height, so this rule drew nothing at all below 1360. */
  .jl__hair { position: absolute; left: 2px; top: 174px; display: block; width: 433px; height: 1px; background: rgba(153, 152, 149, 0.7); }
  .jl__date {
    position: absolute; left: 0; top: 190px;
    font-size: var(--fs-stat); line-height: 43px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-display); color: var(--c-ink-heading);
  }
  .jl__date i {
    margin-left: 13px; font-style: normal;
    font-size: 18px; line-height: 43px; font-weight: var(--fw-medium);
    letter-spacing: var(--ls-display); color: var(--c-ink-heading);
  }
  /* Title beside the date, as the export sets it. Only 34 of the 115 have a
     trustworthy date, so a card without one gives the title the full width
     rather than leaving a 234px hole where a number should be. */
  /* THREE LINES, THEN AN ELLIPSIS. The card is absolutely laid out, so a
     title is the one thing on it that can grow: the long ones ran to five and
     six lines and pushed straight through the disc of the card below. Clamping
     holds every card to the same height, which is what the grid assumes. */
  .jl__title {
    position: absolute; left: 234px; top: 198px; width: 200px;
    display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 3;
    overflow: hidden;
    font-size: 18px; line-height: 27px; font-weight: var(--fw-medium);
    letter-spacing: -0.03em; color: rgb(20, 20, 20);
    transition: color var(--dur-fast) ease;
  }
  .jl__grid a:hover .jl__title { color: var(--c-black); }
  .jl__grid a:not(:has(.jl__date)) .jl__title { left: 0; top: 196px; width: 400px; }
  .jl__disc img { display: block; width: 100%; height: 100%; border-radius: 50%; object-fit: cover; }

  /* ── pager ──────────────────────────────────────────────── */
  .jl__pager {
    display: flex; align-items: center; justify-content: space-between;
    margin-top: 90px; padding-top: 28px; border-top: 0.5px solid var(--c-rule-soft);
  }
  .jl__nums { display: flex; align-items: center; gap: 6px; }
  .jl__nums a, .jl__step {
    display: inline-flex; align-items: center; justify-content: center;
    min-width: 40px; height: 40px; padding: 0 14px; border-radius: var(--r-pill);
    font-size: var(--fs-label); line-height: 20px; font-weight: var(--fw-medium);
    letter-spacing: var(--ls-button); color: rgb(20, 20, 20);
    box-shadow: inset 0 0 0 1px rgba(20, 20, 20, 0.16);
    transition: background var(--dur-fast) ease, box-shadow var(--dur-fast) ease;
  }
  .jl__nums a:hover, .jl__step:hover { box-shadow: inset 0 0 0 1px rgba(20, 20, 20, 0.4); }
  .jl__nums a[aria-current='page'] { background: var(--c-accent); box-shadow: none; }
  /* The ends are not links at the ends. A disabled step keeps its place in
     the row so the numbers do not shift between pages. */
  .jl__step[aria-disabled='true'] {
    color: rgba(20, 20, 20, 0.3); box-shadow: inset 0 0 0 1px rgba(20, 20, 20, 0.08);
    pointer-events: none;
  }

  /* ── below the design grid ──────────────────────────────── */
  @media (max-width: 1360px) {
    .jh__inner { grid-template-columns: 1fr; min-height: 0; padding-block: 120px 72px; row-gap: 36px; }
    .jh__rail, .jh__mast, .jh__count { grid-column: 1; grid-row: auto; justify-self: start; margin-top: 0; width: auto; }
    .jh__statement { width: auto; max-width: 440px; }
    .jh__large { line-height: 1.04; margin-left: 0; }

    .jl { padding-block: 64px 72px; }
    .jl__grid { margin-top: 48px; grid-template-columns: repeat(2, minmax(0, 1fr)); column-gap: 24px; row-gap: 40px; }
    .jl__grid a { min-height: 0; padding-top: 18px; }
    .jf { margin-top: 48px; }
    .jf__pips { top: 20px; right: 24px; }
    .jf__cap { left: 24px; bottom: 24px; }
    .jf__date { width: auto; }
    .jl__disc, .jl__hair, .jl__date, .jl__title { position: static; }
    .jl__grid a:not(:has(.jl__date)) .jl__title { width: auto; }
    .jl__date { display: block; margin-top: 14px; }
    /* 132, not the 88 this started at. At 88 the thumbnail read as an icon
       beside a two-line title rather than as the post's picture, and these
       heroes carry their own headline panel, which at 88px was unreadable. */
    .jl__disc { width: 132px; height: 132px; }
    .jl__hair { width: 100%; margin: 18px 0; }
    .jl__title { width: auto; }
    .jl__pager { flex-wrap: wrap; gap: 16px; margin-top: 56px; }
  }
  @media (max-width: 680px) {
    .jl__grid { grid-template-columns: 1fr; }
  }
</style>

---
/**
 * Homepage. Nine sections, spec in design/specs/Main.md.
 *
 * All copy here is verbatim from that spec, including its own punctuation.
 * Nothing is paraphrased and nothing is invented.
 *
 * MISSING IMAGERY. The spec sources every photograph from
 * roarsinc.com/wp-content/uploads/*. Those files are not in this repo yet:
 * they arrive with the uploads import in Phase 5, and CLAUDE.md forbids
 * hot-linking the live WordPress site. So every photo slot renders the
 * design's own placeholder ground, rgb(30,30,30), at the right dimensions,
 * which keeps the layout and the CLS budget honest. Each one is listed in the
 * build report. Nothing here is a stock stand-in.
 */
import BaseLayout from '../layouts/BaseLayout.astro'
import Section from '../components/Section.astro'
import TopBar from '../components/TopBar.astro'
import NavOverlay from '../components/NavOverlay.astro'
import SiteFooter from '../components/SiteFooter.astro'
import Button from '../components/Button.astro'
import Accordion from '../components/Accordion.astro'
import { site } from '../lib/site'
import { PORTRAIT, CLIENT_LOGOS, workHeroFor, JOURNAL } from '../lib/media'
import GrainField from '../components/GrainField.astro'
/* Decorative furniture still used by About and Insights. The hero's own
   set — planet, starbelt, slash mark — went with the export 2 redesign. */
import star3 from '../assets/star-3.svg'
import star5 from '../assets/star-5.svg'

/* ------------------------------------------------------------ content */

/**
 * ALL TWELVE, IN THE ORDER /s/ PUTS THEM.
 *
 * This band showed four of the twelve and hid the rest behind an accordion
 * nobody was going to open twelve times. It is now a straight list: every
 * service, one row each, and the circle on the right is a link to the page
 * rather than a toggle.
 *
 * The lines are the ones /s/index.astro already writes for each service, not
 * a second set — one description per service, in one place.
 *
 * NO "N PROJECTS" FIGURE. The old rows carried 40, 35 and 17, which are
 * export numbers with nothing behind them in the content: no service record
 * has a project count, and nine of the twelve would have had no number at all.
 * Same call as the "[ NEEDS THE REAL FIGURE ]" placeholders.
 */
const services = [
  { name: 'MVP Development', href: '/s/mvp-development/', body: 'Put one testable version in front of real users and read what they do.' },
  { name: 'Innovation Design', href: '/s/innovation-design-company/', body: 'Turn an ambition everyone agrees with into something specific enough to be wrong.' },
  { name: 'Product Development', href: '/s/product-development-company/', body: 'Strategy, design and engineering as one team, for a product with a roadmap.' },
  { name: 'Mobile App Development', href: '/s/mobile-app-development/', body: 'Native or cross platform, built to survive the second week rather than the review.' },
  { name: 'Web App Development', href: '/s/web-app-development/', body: 'The system that replaces the spreadsheet your operations actually run on.' },
  { name: 'eCommerce Development', href: '/s/ecommerce-development-company/', body: 'Storefront, checkout and the stock integration, tested before the peak.' },
  { name: 'User Experience Design', href: '/s/user-experience-design-agency/', body: 'Find where the product loses people, fix it, and prove the number moved.' },
  { name: 'Growth Hacking', href: '/s/growth-hacking-agency/', body: 'Small bets with a read on each, so spend stops being a renewal of last year.' },
  { name: 'AI Automation', href: '/s/ai-automation-services/', body: 'Let a system own the routine work your team is spending Tuesdays on.' },
  { name: 'DevOps', href: '/s/result-oriented-devops-services/', body: 'Turn a release event into a release routine your own team runs.' },
  { name: 'Hire Dedicated Developers', href: '/s/hire-dedicated-developers/', body: 'Named people on your board, without four months of recruiting first.' },
  { name: 'Digital Transformation', href: '/s/digital-business-transformation-services/', body: 'When four systems disagree and nobody owns the agreement between them.' },
]

/**
 * Two featured case studies. Slugs from docs/URL-INVENTORY.csv.
 *
 * The shot comes from workHeroFor, the same resolver /work/ and /work/<slug>/
 * use, so a project shows the same photograph everywhere it is named and there
 * is one place to change it. It is built at 2:1 by scripts/build-work-headers,
 * which is why .proj__ph is 2:1 below rather than the export's 1129x579.
 */
const featured = [
  {
    day: '02 Jul',
    year: '2025',
    name: 'Parqly.',
    href: '/work/parqly-parking-solution/',
    faces: [PORTRAIT.chetna, PORTRAIT.atul],
  },
  {
    day: '15 Oct',
    year: '2026',
    name: 'Snowman Logistics.',
    href: '/work/warehouse-compliance-checklist-app/',
    faces: [PORTRAIT.abhishek, PORTRAIT.suzanne],
  },
].map((f) => ({ ...f, img: workHeroFor(f.href) }))

/**
 * The cascade. Four testimonials at four sizes and four vertical offsets,
 * measured off the rendered prototype. The irregularity IS the design, so the
 * per-item geometry lives here as data rather than as four hand-tuned CSS
 * blocks: `col` places it in the 7-track grid, the rest drive one shared rule
 * set through custom properties.
 */
const testimonials = [
  {
    col: 5, top: 0, avatar: 80, label: 14, qs: 24, lead: 32, star: 14, photo: PORTRAIT.suzanne,
    name: 'Jayeis Sonill',
    role: 'CEO, Aum Investment',
    quote:
      'Roars has consistently positioned themselves at the forefront of innovation and technology. They understand business needs and craft technologies to address them.',
  },
  {
    col: 3, top: 264, avatar: 65, label: 13, qs: 18, lead: 25, star: 12, photo: PORTRAIT.abhishek,
    name: 'James Hadley',
    role: 'CEO, Simthing New, LLC',
    quote:
      'I was pleased with the deliverables. I was able to see and understand the efforts required to scale the solution in development.',
  },
  {
    col: 1, top: 108, avatar: 57, label: 12, qs: 15, lead: 21, star: 11, photo: PORTRAIT.chetna,
    name: 'Avinash Kumar',
    role: 'Project Manager, Roars',
    quote:
      'From discovery to launch, the team brought our platform to life with precision. A genuine product partner.',
  },
  {
    col: 7, top: 230, avatar: 40, label: 10, qs: 12, lead: 17, star: 9, photo: PORTRAIT.khusboo,
    name: 'Khusboo Panchal',
    role: 'Digital Marketing',
    quote: 'Fast, smart and beautiful. We will be back for round two soon.',
  },
]

const industries = [
  { letter: 'R', name: 'Restaurant', href: '/industries/food-restaurant-app-development/' },
  { letter: 'F', name: 'Fitness', href: '/industries/on-demand-fitness-app-development/' },
  { letter: 'e', name: 'eCommerce', href: '/industries/retail-ecommerce-development/' },
  { letter: 'C', name: 'Concierge', href: '/industries/concierge-app-development/' },
  { letter: 'T', name: 'Travel', href: '/industries/travel-and-hospitality-app-development/' },
  { letter: 'L', name: 'Logistics', href: '/industries/logistics-transportation-app-development/' },
  { letter: 'S', name: 'SaaS', href: '/industries/saas-application-development-services/' },
  { letter: 'H', name: 'Healthcare', href: '/industries/healthcare-app-development-company/' },
]

/** Slugs resolved against docs/URL-INVENTORY.csv, not invented. */
const insights = [
  {
    day: '15',
    month: 'May',
    title: 'AI App Development vs Traditional App Development',
    img: JOURNAL.aiVsTraditional,
    href: '/our-journal/ai-app-development-vs-traditional-app-development-which-is-better-for-customer-engagement/',
  },
  {
    day: '04',
    month: 'May',
    title: '20 Years of Roars: Built on Purpose, Driven by Impact',
    img: JOURNAL.twentyYears,
    href: '/our-journal/20-years-of-roars-built-on-purpose-driven-by-impact/',
  },
  {
    day: '11',
    month: 'Dec',
    title: 'The Unexpected Insight: we built a meal planning app',
    img: JOURNAL.mealPlanning,
    href: '/our-journal/the-unexpected-insight-we-built-a-meal-planning-app/',
  },
]

const faq = [
  {
    question: 'How long does it take to build a product?',
    answer:
      'The timeline depends on complexity and specific requirements. An MVP typically runs eight to sixteen weeks. We provide a detailed timeline during the initial consultation to ensure clear expectations.',
  },
  {
    question: 'What’s included in your product strategy?',
    answer:
      'Discovery workshops, market and competitor review, product definition, technical feasibility and a costed delivery roadmap.',
  },
  {
    question: 'How does the monthly engagement model work?',
    answer:
      'A dedicated team bills monthly and works to your cadence. You can scale up or down at the end of any month, with a named project manager throughout.',
  },
  {
    question: 'Can you take over an existing product?',
    answer:
      'Yes. We start with a technical and design audit, then agree a plan to stabilise what exists before extending it.',
  },
  {
    question: 'Do you work with pre-seed startups?',
    answer:
      'Regularly. Much of our work is with founders building a first version, and we scope to the funding available.',
  },
]

/* ------------------------------------------------------------- schema */

/**
 * Organization + WebSite, homepage only, per docs/SEO-SPEC.md 7.1.
 * FAQPage is included because the accordion below renders the real Q&A in the
 * DOM and the answers match word for word, which is the condition for it.
 * No aggregateRating: there are no verifiable on-page reviews.
 */
const schema = [
  {
    '@context': 'https://schema.org',
    '@graph': [
      {
        '@type': ['Organization', 'ProfessionalService'],
        '@id': `${site.url}/#organization`,
        name: site.name,
        legalName: site.legalName,
        alternateName: site.shortName,
        url: `${site.url}/`,
        foundingDate: site.founded,
        founder: { '@type': 'Person', name: 'Rinkesh A Shah', jobTitle: 'Founder and CEO' },
        sameAs: site.sameAs,
        email: site.email.sales,
        contactPoint: [
          {
            '@type': 'ContactPoint',
            telephone: site.phones.US.tel,
            contactType: 'sales',
            areaServed: ['US', 'CA'],
            availableLanguage: ['en'],
          },
          {
            '@type': 'ContactPoint',
            telephone: site.phones.UK.tel,
            contactType: 'sales',
            areaServed: ['GB', 'EU'],
            availableLanguage: ['en'],
          },
        ],
        address: site.offices.map((o) => ({
          '@type': 'PostalAddress',
          addressLocality: o.city,
          ...(o.region ? { addressRegion: o.region } : {}),
          addressCountry: o.country,
        })),
        knowsAbout: [
          'Product Development',
          'User Experience Design',
          'Mobile App Development',
          'MVP Development',
          'AI Automation',
          'Digital Transformation',
        ],
        numberOfEmployees: { '@type': 'QuantitativeValue', minValue: 11, maxValue: 50 },
      },
      {
        '@type': 'WebSite',
        '@id': `${site.url}/#website`,
        url: `${site.url}/`,
        name: site.name,
        publisher: { '@id': `${site.url}/#organization` },
        inLanguage: 'en-US',
      },
    ],
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faq.map((f) => ({
      '@type': 'Question',
      name: f.question,
      acceptedAnswer: { '@type': 'Answer', text: f.answer },
    })),
  },
]
---

<BaseLayout
  title="Product Development for Startups & SMEs | Roars"
  description="Roars is a product development and design agency. 250+ products shipped since 2005, across product strategy, UI/UX, mobile apps and AI automation."
  path="/"
  ogType="website"
  schema={schema}
>
  <TopBar slot="topbar" />
  <NavOverlay />

  {/* ── 1. Header ───────────────────────────────────────────── */}
  <Section ground="accent" bleed>
    {/* Export 2 replaces the space photograph with a full-bleed yellow
        ground. CLAUDE.md's "yellow is never a ground" rule was updated to
        allow exactly this, and only where the prototype does it. The old
        hero — panel, planet, starbelt, slash mark, wordmark, badge and the
        services rail — is gone with it, and space.jpg with that. */}
    <div class="hero">
      <GrainField tone="yellow" second />

      <div class="wrap hero__inner">
        <h1 class="hero__h1">
          Ship product<br />that{' '}
          <span class="hero__rotate" data-rotate data-words="scales|lasts|earns|works">scales</span>
        </h1>
        <p class="hero__lede">
          Roars is a product studio working out of the USA, UK and India.
          250 products shipped for startups and SMEs since 2005.
        </p>
        <a class="hero__cta" href="/contact-us/">
          Start a project<span class="hero__dot" aria-hidden="true"></span>
        </a>
        <div class="hero__rule" aria-hidden="true"></div>
        <ul class="hero__labels">
          <li>Product development</li>
          <li>UI/UX &amp; product design</li>
          <li>Mobile apps</li>
          <li>MVP in 12 weeks</li>
          <li>AI automation</li>
        </ul>
      </div>
    </div>
  </Section>

  {/* ── 2. About ────────────────────────────────────────────── */}
  <Section ground="light">
    <div class="about">
      {/* The paired star marks in the left rail. Decoration, so aria-hidden. */}
      <span class="about__stars" aria-hidden="true">
        <span class="starmark" style={`--m:url(${star3.src});width:20px;height:20px`}></span>
        <span class="starmark" style={`--m:url(${star5.src});width:20px;height:20px`}></span>
      </span>
      {/* All THREE stats live in one 1362x362 block in the export, and the
          third is nested inside it at an offset of 698,187 — it is not a
          second row under the first two, which is how it was built and why it
          sat 15px low with 2005 18px adrift. The two placements overlap in
          the same grid area, which is what grid overlap is for; nothing here
          is absolutely positioned. Columns 463/235/664 sum to the 1362
          content box, so they hold at any width. */}
      <div data-reveal class="about__stats">
        <div class="about__stat-pair">
          <div class="about__stat">
            <p class="about__stat-name">Projects Delivered</p>
            <div class="rule about__stat-rule"></div>
            <div class="about__stat-row">
              <span class="about__stat-num">250<span class="about__plus">+</span></span>
              <span class="about__stat-note">Products launched successfully since 2005</span>
            </div>
          </div>
          <div class="about__stat">
            <p class="about__stat-name">Returning Customers</p>
            <div class="rule about__stat-rule"></div>
            <div class="about__stat-row">
              <span class="about__stat-num">96<span class="about__pct">%</span></span>
              <span class="about__stat-note">Clients who return for their next product</span>
            </div>
          </div>
        </div>

        <div class="about__est">
          <p class="about__est-name">Year of Establishment</p>
          <div class="rule about__est-rule"></div>
          <div class="about__est-row">
            <span class="about__est-note">
              The year Rinkesh A Shah founded Roars, a first product for an
              early-stage startup.
            </span>
            <span class="about__est-side">
              <span class="about__stat-num about__est-num">2005</span>
              {/* Two 28px portraits under the year. Files arrive with the
                  Phase 5 uploads import; the discs hold their places. */}
              <span class="about__est-faces" aria-hidden="true">
                <img src={PORTRAIT.suzanne} alt="" width="28" height="28" loading="lazy" />
                <img src={PORTRAIT.nitin} alt="" width="28" height="28" loading="lazy" />
              </span>
            </span>
          </div>
        </div>
      </div>

      <div data-reveal class="about__team" style="transition-delay:120ms">
        <p class="about__team-name">Team of Roars</p>
        <div class="rule rule--short"></div>
        {/* Scattered cluster, not a row: the prototype offsets the four
            portraits on a 2x3 lattice with one cell left empty, and the count
            sits in that gap. Portraits arrive with the Phase 5 uploads
            import; the discs hold their places until then. */}
        <div class="about__cluster">
          <img class="about__face about__face--a" src={PORTRAIT.suzanne} alt="" width="42" height="42" loading="lazy" />
          <img class="about__face about__face--b" src={PORTRAIT.chetna} alt="" width="42" height="42" loading="lazy" />
          <img class="about__face about__face--c" src={PORTRAIT.nitin} alt="" width="42" height="42" loading="lazy" />
          <img class="about__face about__face--d" src={PORTRAIT.atul} alt="" width="42" height="42" loading="lazy" />
          <p class="about__team-count"><span>59</span></p>
        </div>
      </div>

      <div data-reveal class="about__who" style="transition-delay:160ms">
        <p class="about__who-name">Roars</p>
        <p class="about__who-sub">Product Company</p>
        <img class="about__who-face" src={PORTRAIT.founder} alt="" width="60" height="60" loading="lazy" />
        <p class="about__who-person">Rinkesh A Shah</p>
        <p class="about__who-role">CEO / Founder</p>
      </div>

      {/* The pitch and the logo strip share one column in the export — both
          sit in the block that opens at 584, at 79 and 296 into it. Given
          their own grid rows they were separated by the row gap and the strip
          landed 45px low. */}
      <div class="about__right">
        <div data-reveal class="about__pitch" style="transition-delay:200ms">
          <p>
            We work with you as thinkers, creators, consultants, designers and
            implementers{' '}
            <span class="about__pitch-mid">to turn your product into a high quality marketable offering.</span>{' '}
            <span class="about__pitch-low">Our approach is simple. We bring clarity to complex situations.</span>
          </p>
        </div>

        {/* Client logos.

            DELIBERATE CHANGE FROM THE EXPORT, asked for directly. The export
            draws a static flex row inside a 629px overflow:hidden box, so
            with the real files at their drawn heights only Samsung, TATA and
            GISAID ever appear and Forbes, Reliance and DDB are clipped off
            the right edge — six marks paid for, three seen. It rotates now,
            and the marks are smaller so more of the strip reads at once.

            The row is duplicated and the track travels -50%, which is what
            makes the loop seamless; the export's own edge fades cover both
            entry and exit. The clone is aria-hidden with empty alts so the
            the brands are announced once. */}
        <div class="about__logos" data-reveal>
          <div class="about__logo-track">
            {[false, true].map((clone) => (
              <div class="about__logo-row" aria-hidden={clone ? 'true' : undefined}>
                {CLIENT_LOGOS.map((l, i) => (
                  <img
                    src={l.src}
                    alt={clone ? '' : l.alt}
                    style={`--i:${i}`}
                    loading="lazy"
                    decoding="async"
                  />
                ))}
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </Section>

  {/* ── 3. Services ─────────────────────────────────────────── */}
  <Section ground="surface">
    <div class="svc">
      <h2 class="display">Services</h2>

      {/* Accordion. Approved island 2 of 4, shared with the FAQ below.

          THE ROWS ALTERNATE. Odd rows take the wide inner track — name at 502,
          body at 966, toggle at the far right — and even rows take the narrow
          rail track: name at 39, body at 503, toggle at 901. A zigzag.

          This was previously flattened to the rail on every row and logged as
          artboard drift, on the reasoning that two rows disagreed with two
          rows and there was no majority. There was no majority because it
          alternates. Where the prototype is irregular, the irregularity is
          the design; see CLAUDE.md. */}
      {/* A LIST, NOT AN ACCORDION ANY MORE.

          It was four rows with the first one open, and eight services missing
          entirely. Twelve rows that each open a panel is not a thing anybody
          works through, so the panel is gone: the circle on the right is now
          a link straight to the service page, which is where the toggle was
          only ever a detour on the way to.

          WHAT STAYS IS THE ZIGZAG. The export's own rhythm: odd rows start at
          the rail (464), even rows at the container edge, and every row runs
          name(465) desc(232) spare(166) link(36). The spare track is kept
          rather than collapsed so the circle stays exactly where the export
          puts it, at the right-hand edge.

          The whole row is the link target, not just the circle — a 36px
          hit area at the end of a 1364px row is a bad thing to ask for. The
          circle is the affordance; the <a> is the row. */}
      <ul class="svc__list">
        {services.map((s, i) => (
          <li class:list={['svc__row', i % 2 === 0 ? 'svc__row--inner' : 'svc__row--rail']}>
            {/* Rules are ELEMENTS, not a border-top on the row. A border spans
                the row's whole box, and the box stays the full 1364 even when
                the tracks are packed left — so every row drew one rule from
                38 to 1400, which is the first thing that looks wrong here. */}
            <span class="svc__rule" aria-hidden="true"></span>
            {/* The link is an overlay across the whole row rather than a
                wrapper around the other three. A wrapper would have to be
                `display: contents` to let them keep their grid tracks, and an
                interactive element with no box is exactly the thing screen
                readers have historically dropped. This keeps a real box, a
                real focus ring, and the row's geometry untouched.

                FIRST in the row, not last: the hover and focus styles reach
                the name and the circle with `~`, and a sibling combinator only
                looks forwards. */}
            <a class="svc__link" href={s.href}>{s.name}</a>
            <h3 class="svc__name">{s.name}</h3>
            <p class="svc__body">{s.body}</p>
            <span class="svc__icon" aria-hidden="true"></span>
          </li>
        ))}
      </ul>

      <div class="svc__foot">
        <p class="intro">
          Roars helps founders stand out with bold product design and smart
          digital solutions. We mix strategy, creativity and technology.
        </p>
        <Button href="/contact-us/" label="Start a Project" />
      </div>
    </div>
  </Section>

  {/* ── 4. Projects ─────────────────────────────────────────── */}
  <Section ground="dark" bleed>
    <div class="proj">
    <GrainField tone="dark" />

    <div class="wrap proj__inner">
      <div class="proj__head">
        <h2 class="display display--on-dark">Projects</h2>
        <p class="proj__count">250<span>+</span></p>
        <ul class="proj__legend">
          {['Recent Projects', 'Product Development', 'UI/UX and Product Design', 'Mobile and AI'].map((l) => (
            <li><span>{l}</span><i aria-hidden="true"></i></li>
          ))}
        </ul>
      </div>

      {/* No inline visibility on the slides: each card has to stay paintable
          so the incoming image can ride up OVER the outgoing one, which is
          the whole device. Only the text hides, and it hides discretely — see
          .proj__slide[aria-hidden] below and scripts/crossfade.ts. */}
      <div class="proj__stage" data-crossfade>
        {featured.map((f, i) => (
          <article
            class="proj__slide"
            data-slide
            style={`opacity:${i === 0 ? 1 : 0}`}
            aria-hidden={i === 0 ? undefined : 'true'}
          >
            <div class="proj__date">
              <span class="proj__day">{f.day}</span>
              <div class="rule rule--thin"></div>
              <span class="proj__year">{f.year}</span>
            </div>
            <a class="proj__card" href={f.href}>
              <img class="proj__ph" src={f.img} alt="" width="2400" height="1200" loading="lazy" decoding="async" />
              <span class="proj__faces" aria-hidden="true">
                {f.faces.map((src) => <img src={src} alt="" width="42" height="42" loading="lazy" />)}
              </span>
              <span class="proj__name">{f.name}</span>
            </a>
          </article>
        ))}
      </div>

      <div class="proj__foot">
        <span class="proj__stars" aria-hidden="true">
          <span class="starmark" style={`--m:url(${star3.src});width:17px;height:17px`}></span>
          <span class="starmark" style={`--m:url(${star5.src});width:17px;height:17px`}></span>
        </span>
        <Button href="/work/" label="All Projects" variant="light" />
      </div>
    </div>
    </div>
  </Section>

  {/* ── 5. People say ───────────────────────────────────────── */}
  <Section ground="light">
    <div class="say">
      <h2 class="display">People say</h2>
      <p class="say__score">4.9/5</p>

      {/* A cascade, not a row of four equal cards. The four columns do not
          overlap horizontally, so this is one grid with uneven tracks plus a
          per-item top offset — flow layout, no absolute positioning. */}
      <ul class="say__list">
        {testimonials.map((t, i) => (
          <li
            class="say__item"
            data-reveal
            style={`--col:${t.col};--top:${t.top}px;--av:${t.avatar}px;--lb:${t.label}px;--qt:${t.qs}px;--ld:${t.lead}px;--st:${t.star}px;transition-delay:${i * 70}ms`}
          >
            <img class="say__avatar" src={t.photo} alt="" width={t.avatar} height={t.avatar} loading="lazy" decoding="async" />
            <p class="say__name">{t.name}</p>
            <p class="say__role">{t.role}</p>
            <div class="say__rule"></div>
            <div class="say__stars" aria-label="Rated 5 out of 5">
              {[0, 1, 2, 3, 4].map(() => (
                <span class="starmark" style={`--m:url(${star5.src});width:var(--st);height:var(--st)`} aria-hidden="true"></span>
              ))}
            </div>
            <blockquote class="say__quote">{t.quote}</blockquote>
          </li>
        ))}
      </ul>

      <div class="say__foot">
        <div class="say__nav">
          <span class="say__dot"></span>
          <span class="say__prev">Prev.</span>
          <span class="say__next">Next</span>
          <span class="say__dot say__dot--on"></span>
        </div>
        {/* Reviewer portraits arrive with the Phase 5 uploads import; the
            count disc closes the stack, as in the prototype. */}
        <div class="say__stack">
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
          <span class="say__count">37</span>
        </div>
        <p class="say__meta">reviews<br />4.9 average</p>
        <Button href="/contact-us/" label="Leave a Review" />
      </div>
    </div>
  </Section>

  {/* ── 6. Industries ───────────────────────────────────────── */}
  <Section ground="surface">
    <div class="ind">
      <h2 class="display">Industries</h2>
      {/* Label pair lives in the left rail, not the inner column. */}
      <div class="ind__rail">
        <p class="ind__label">Eight sectors</p>
        <p class="ind__sub">Where we work</p>
      </div>
      <p class="intro ind__intro">
        We build in sectors we already understand, so the discovery work starts
        from domain knowledge rather than from scratch.
      </p>

      <ul class="ind__grid">
        {industries.map((sector, i) => (
          <li data-reveal style={`transition-delay:${i * 50}ms`}>
            <a class="ind__card" href={sector.href}>
              <span class="ind__letter" aria-hidden="true">{sector.letter}</span>
              <span class="ind__text">
                <span class="ind__name">{sector.name}</span>
                <span class="ind__cta">View sector</span>
              </span>
            </a>
          </li>
        ))}
      </ul>

      <div class="ind__foot">
        <span class="ind__stars" aria-hidden="true">
          <span class="starmark" style={`--m:url(${star3.src});width:17px;height:17px`}></span>
          <span class="starmark" style={`--m:url(${star5.src});width:17px;height:17px`}></span>
        </span>
        <Button href="/work/" label="See the work" />
      </div>
    </div>
  </Section>

  {/* ── 7. Insights ─────────────────────────────────────────── */}
  <Section ground="light">
    <div class="ins">
      <h2 class="display">Insights</h2>
      <div class="ins__head">
        <div class="ins__who">
          <img class="ins__avatar" src={PORTRAIT.founder} alt="" width="42" height="42" loading="lazy" />
          <p class="ins__name">Rinkesh A Shah</p>
          <p class="ins__role">Founder</p>
        </div>
        <p class="intro ins__intro">
          We have a lot to say about product work: how we approach the process,
          what the AI shift actually changes, and the nuances of building for
          founders.
        </p>
      </div>

      <ul class="ins__grid">
        {insights.map((post, i) => (
          <li data-reveal style={`transition-delay:${i * 70}ms`}>
            <a class="ins__card" href={post.href}>
              <img class="ins__ph" src={post.img} alt="" width="138" height="138" loading="lazy" decoding="async" />
              <span class="ins__rule"></span>
              <span class="ins__day">{post.day}</span>
              <span class="ins__month">{post.month}</span>
              <span class="ins__title">{post.title}</span>
            </a>
          </li>
        ))}
      </ul>

      <div class="ins__foot">
        <span class="ins__stars" aria-hidden="true">
          <span class="starmark" style={`--m:url(${star3.src});width:17px;height:17px`}></span>
          <span class="starmark" style={`--m:url(${star5.src});width:17px;height:17px`}></span>
        </span>
        <Button href="/our-journal/" label="All Insights" />
      </div>
    </div>
  </Section>

  {/* ── 8. FAQ ──────────────────────────────────────────────── */}
  <Section ground="surface">
    <div class="faq">
      <h2 class="display">FAQ</h2>
      {/* Founder block sits in the left rail, as in the prototype. */}
      <div class="faq__rail">
        <img class="faq__avatar" src={PORTRAIT.founder} alt="" width="42" height="42" loading="lazy" />
        <p class="faq__who">Rinkesh A Shah</p>
        <p class="faq__role">CEO / Founder</p>
        <p class="faq__intro">
          Behind every project: the thinking that fuels how Roars builds.
        </p>
      </div>

      <Accordion items={faq} name="home-faq" />

      <div class="faq__foot">
        <p class="faq__ask">Didn’t find the answer? Ask us about our services!</p>
        <Button href="/contact-us/" label="Ask your Question" />
      </div>
    </div>
  </Section>

  {/* ── 9. Footer ───────────────────────────────────────────── */}
  <SiteFooter slot="footer" />
</BaseLayout>

<style>
  /* shared ------------------------------------------------------------- */
  .rule { height: 1.5px; background: var(--c-rule-strong); }
  .rule--short { width: 16px; }
  .rule--thin { height: 1.25px; }
  /* A clamp's vw term has to REACH its cap at the 1440 design width, or the
     cap never applies and the step lands fractionally small. 8.6vw is 123.84
     at 1440, not 124 — and since letter-spacing and any em-sized glyph
     children are relative, one missing 0.16px reappears as a row per
     descendant in the diff table. 124/1440 = 8.611vw, so 8.62vw overshoots by
     a hair and the cap decides. Same rule at every clamp here: the vw term
     must exceed cap/14.4. */
  .display {
    margin: 0;
    font-size: clamp(var(--fs-heading-m), 8.62vw, var(--fs-heading));
    font-weight: var(--fw-semi);
    line-height: var(--lh-tight);
    letter-spacing: var(--ls-display);
    color: var(--c-ink-heading);
  }
  .display--on-dark { color: var(--c-off, #fbfbfb); }
  .intro {
    font-size: var(--fs-intro);
    font-weight: var(--fw-medium);
    line-height: var(--lh-intro);
    letter-spacing: var(--ls-item);
    /* The prototype sets section intros in near-black, not the heading ink. */
    color: var(--c-black);
  }

  /* 1. hero ------------------------------------------------------------ */
  .hero { position: relative; min-height: 900px; display: flex; align-items: stretch; }

  .hero__inner {
    position: relative; z-index: 1; width: 100%;
    display: flex; flex-direction: column; align-items: flex-start;
    padding-block: 200px 0;
  }
  .hero__h1 {
    max-width: 1180px; margin: 0;
    font-size: clamp(56px, 10vw, 144px);
    font-weight: var(--fw-semi);
    line-height: 130px;
    letter-spacing: -0.055em;
    color: var(--c-footer);
  }
  .hero__rotate { display: inline-block; min-width: 360px; transition: opacity 260ms var(--ease-out); }
  .hero__lede {
    max-width: 600px; margin: 60px 0 0;
    font-size: var(--fs-item); line-height: 34px;
    font-weight: var(--fw-medium); letter-spacing: var(--ls-item);
    color: rgba(11, 11, 11, 0.78);
  }
  .hero__cta {
    display: inline-flex; align-items: center; gap: 11px;
    height: 52px; margin-top: 26px; padding-inline: 30px;
    border-radius: var(--r-pill);
    background: var(--c-footer); color: var(--c-white);
    font-size: 15px; font-weight: var(--fw-semi); letter-spacing: var(--ls-button);
  }
  .hero__dot { width: 7px; height: 7px; border-radius: 50%; background: var(--c-accent); }
  .hero__rule { width: 100%; height: 1px; margin-top: 74px; background: rgba(11, 11, 11, 0.22); }
  .hero__labels {
    display: flex; justify-content: space-between;
    width: 100%; margin: 23px 0 0; padding: 0;
    list-style: none;
    font-size: 13px; font-weight: var(--fw-semi);
    letter-spacing: 0.14em; text-transform: uppercase;
    /* The export sets no line-height here and inks at 72%, not solid. */
    line-height: normal;
    color: rgba(11, 11, 11, 0.72);
  }

  /* 2. about ----------------------------------------------------------- */
  .about__stars { position: absolute; left: 3px; top: 139px; display: flex; gap: 6px; }
  .about {
    position: relative;
    display: grid;
    grid-template-columns: 464fr 234fr 234fr 430fr;
    /* 83, not --sp-6: the export's stats block ends at 501 and the team row
       opens at 584. */
    row-gap: 83px;
    column-gap: 0;
    /* 155, not 123: the export's last block ends at 924 in a 1079 section. */
    padding-block: 139px 155px;
  }
  /* The export's one stats block: 1362x362 at 39,139. Two placements share
     it — the pair from column 463, the Year block from 698,187. Tracks are
     fr so they resolve to 463/235/664 on the 1362 content box and stay
     proportional below it. */
  .about__stats {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: 463fr 235fr 664fr;
    grid-template-rows: 187px minmax(0, 1fr);
    min-height: 362px;
  }
  .about__stat-pair {
    grid-column: 2 / -1; grid-row: 1 / -1;
    display: grid; grid-template-columns: 435fr 29fr 435fr;
  }
  .about__stat-pair > :last-child { grid-column: 3; }
  .about__est { grid-column: 3 / -1; grid-row: 2 / -1; }

  /* Inside a stat box: name 0, rule 43, number 54, note 73. The name and the
     rule carry the export's 2px inset; the number does not. */
  .about__stat-name, .about__est-name {
    font-size: var(--fs-intro); line-height: 19px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-body); color: var(--c-black);
  }
  .about__stat-name { margin-left: 2px; }
  .about__stat-rule { margin: 24px 0 0 2px; }
  /* Both tracks are fr and they SUM to the box. Pairing a large fr with 1fr
     does not mean "235 then the rest" — fr distributes proportionally, so
     235fr 1fr gives the second track 1/236th of the row and the note
     collapsed to 2px. */
  .about__stat-row {
    display: grid;
    grid-template-columns: 235fr 200fr;
    align-items: start;
    margin-top: 9.5px;
  }
  /* Year block: name 0, rule 38, row 60. The row holds the note at 0 and a
     right column whose year overhangs it by 11px, exactly as drawn. */
  .about__est-rule { margin-top: 19px; }
  /* The drawn row is 448 of the block's 664; the trailing track is the
     remainder so the first two land at 228 and 220. */
  .about__est-row {
    display: grid; grid-template-columns: 228fr 220fr 216fr;
    align-items: start; margin-top: 20.5px;
  }
  .about__est-num { line-height: 84px; }
  /* Flex, not block: as a block the 84px inline span sits in a line box with
     the 22px strut and the two align on the baseline, which put the year 9px
     high. A flex column makes the span its own item. */
  .about__est-side { display: flex; flex-direction: column; align-items: flex-start; margin-top: -11px; }
  .about__est-faces { display: flex; gap: 6px; margin-top: 9px; margin-left: 2px; }
  .about__est-faces img {
    width: 28px; height: 28px; border-radius: var(--r-circle);
    object-fit: cover; background: rgb(173, 173, 173);
  }
  .about__stat-num {
    /* 5.8vw is 83.52 at 1440, so the 84px cap never applied. See .display. */
    font-size: clamp(var(--fs-stat-m), 5.84vw, var(--fs-display));
    /* 84/84 in the export. 43 was a mispairing — it is the /Projects count
       in the Services rows that leads at 43 — and the 41px it cost is
       exactly what left the About section short and shifted every section
       below it. */
    font-weight: var(--fw-semi); line-height: 84px;
    letter-spacing: var(--ls-display); color: var(--c-black);
    position: relative; display: block;
  }
  /* The export PLACES these — + at 152,71 and % at 110,73 against a number
     that starts at 0,54 — it does not run them on after the digits. Set
     inline they closed up tight against the figure and sat low on its
     baseline. No table of text positions would have caught it: visual-diff
     skips inline boxes, whose rect is a union of line boxes. It took looking
     at the two renders side by side.
     letter-spacing is re-declared because it INHERITS AS A COMPUTED PX value,
     so both were carrying the number's -4.2px rather than their own. */
  .about__plus, .about__pct {
    position: absolute;
    line-height: 31px;
    letter-spacing: var(--ls-display);
  }
  .about__plus { left: 152px; top: 17px; font-size: 36px; font-weight: var(--fw-medium); }
  .about__pct  { left: 110px; top: 19px; font-size: 26px; font-weight: 700; }
  .about__stat-note, .about__est-note {
    max-width: 170px; margin-top: 19px;
    font-size: var(--fs-body); line-height: 22px;
    letter-spacing: var(--ls-body); color: rgb(71, 71, 71); opacity: 0.78;
  }
  /* The 19px drop belongs to the two stat notes, which sit that far under
     their number. The Year note opens its own row and starts at its top. */
  .about__est-note { margin-top: 0; line-height: 23px; }
  .about__est-name { font-size: 18px; color: rgb(54, 53, 53); }
  /* The export sets these three at 24px on a 19px leading — tighter than the
     glyphs, which is what keeps "Roars / Product Company" reading as one
     stacked lockup rather than two lines of copy. --lh-item (32px) is the
     body pairing and is wrong here; it cost 4px on "Roars" and 6px on the
     line under it. */
  .about__team-name, .about__who-name, .about__who-sub {
    font-size: var(--fs-item); line-height: 19px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-item); color: var(--c-black);
  }
  /* Offsets inside the block, from the export: name 0, sub 30, portrait 85,
     name 157, role 178. They are not a rhythm, so each carries its own gap. */
  .about__who { grid-column: 2 / 3; }
  .about__who-sub { margin-top: 11px; color: rgb(71, 71, 71); opacity: 0.7; }
  .about__who-person { margin-top: 12px; font-size: var(--fs-label); line-height: var(--lh-label); font-weight: var(--fw-semi); letter-spacing: var(--ls-body); color: rgb(22, 22, 22); }
  .about__who-role { margin-top: 2px; font-size: var(--fs-label); line-height: var(--lh-label); font-weight: var(--fw-medium); letter-spacing: var(--ls-body); color: rgb(145, 144, 142); }
  /* Team block: name 0, rule 43, cluster 85. */
  .about__team .rule--short { margin-top: 24px; background: rgb(180, 174, 174); }
  .about__cluster { position: relative; width: 145px; height: 143px; margin-top: 40.5px; }
  .about__face {
    position: absolute; width: var(--avatar); height: var(--avatar);
    border-radius: var(--r-circle); object-fit: cover; background: rgb(173, 173, 173);
  }
  .about__face--a { left: 103px; top: 0; }
  .about__face--b { left: 52px;  top: 50px; }
  .about__face--c { left: 0;     top: 101px; }
  .about__face--d { left: 103px; top: 101px; }
  /* One rule, not two. There was a second .about__team-count above the
     cluster block that set 18px on a grey chip; this one overrode it and
     shipped the counter at 14px/19. The export is 18px on a 43px leading. */
  .about__team-count {
    position: absolute; left: 0; top: 0;
    width: var(--avatar); height: var(--avatar);
    margin: 0; border-radius: var(--r-circle);
    background: rgba(153, 152, 149, 0.18);
  }
  /* Not centred — the export sets the numeral at 10,8 on a 43px leading,
     which reads slightly high and left in the disc. Centring it moved the
     figure 33px and lost the relationship to the star mark beside it. */
  .about__team-count > span {
    position: absolute; left: 10px; top: 8px;
    font-size: 18px; line-height: 43px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-item); color: var(--c-black);
  }

  /* Edge fades so the row reads as a strip that continues past its box. */
  .about__logos { position: relative; width: 94.73%; margin: 81px 0 0 -1.81%; overflow: hidden; }
  .about__logos::before, .about__logos::after {
    content: ''; position: absolute; top: 0; bottom: 0; width: 54px; z-index: 1;
  }
  .about__logos::before { left: 0; background: linear-gradient(270deg, rgba(255,255,255,0) 0%, var(--c-white) 100%); }
  .about__logos::after { right: 0; background: linear-gradient(90deg, rgba(255,255,255,0) 0%, var(--c-white) 100%); }
  /* One treatment for every mark, which is the point of the row: grayscale at
     62%, full colour under the cursor. The heights keep the export's relative
     proportions (22/27/24/22/27/22 — not one number, because forcing one
     makes TATA and Reliance look small) scaled to 0.73, which is the "make
     them smaller" half of the request.

     THE ROTATION IS A DELIBERATE CHANGE, not fidelity. See the markup. The
     track is two identical rows and travels exactly -50%, so the moment it
     wraps, row two is where row one began and the loop is invisible. Duration
     scales with the content, not the clock: 6 marks at ~5s each. */
  .about__logo-track {
    display: flex;
    width: max-content;
    animation: logoMarquee 30s linear infinite;
  }
  .about__logos:hover .about__logo-track,
  .about__logos:focus-within .about__logo-track { animation-play-state: paused; }
  @keyframes logoMarquee {
    from { transform: translate3d(0, 0, 0); }
    to   { transform: translate3d(-50%, 0, 0); }
  }
  .about__logo-row {
    display: flex; align-items: center; gap: 36px;
    height: 44px; padding-inline: 18px;
  }
  /* The entrance stagger is KEPT alongside the rotation. The two do not fight:
     the stagger is a Y lift and a fade on each mark, the marquee an X
     translate on the track above them, so they compose. The row still arrives
     as one thing moving rather than six things appearing, and then it turns. */
  /* ONE HEIGHT FOR ALL FIFTEEN, not a per-logo number.
     The old six each carried their own --logo-h because they were raw files
     at whatever size they happened to be. The set is levelled at build time
     now (scripts/build-client-logos.mjs fits each mark's longest axis to the
     same box), so a per-mark override would undo the levelling it is meant to
     preserve. max-width stops a wide wordmark like bollywoodhungama.com from
     running the row long. */
  .about__logo-row img {
    flex: 0 0 auto;
    width: auto; height: 26px; max-width: 132px;
    object-fit: contain;
    opacity: 0.55;
    translate: 0 6px;
    transition:
      filter var(--dur-fast) ease,
      opacity var(--dur-mid) ease calc(var(--i) * 70ms),
      translate var(--dur-mid) cubic-bezier(0.16, 1, 0.3, 1) calc(var(--i) * 70ms);
  }
  /* Visible by default. Gating opacity on .is-in meant that if the observer
     never fired the row simply was not there. The reveal supplies the lift. */
  .about__logos.is-in .about__logo-row img { translate: 0 0; }
  .about__logo-row img:hover { filter: grayscale(0); opacity: 1; }

  /* A strip that moves on its own is exactly what this switches off. It still
     clips at 629, so the export's own three-and-a-bit is what shows. */
  @media (prefers-reduced-motion: reduce) {
    .about__logo-track { animation: none; }
    .about__logo-row img { translate: 0 0; transition: filter var(--dur-fast) ease; }
  }

  /* Explicit box, not the attributes: tokens.css sets img{height:auto}, so a
     portrait whose file is still missing collapses and takes the two names
     under it up with it. The export puts this at 85 in the block, which is
     36 under the line above. */
  .about__who-face { display: block; width: 60px; height: 60px; margin-top: 36px; border-radius: var(--r-circle); object-fit: cover; background: rgb(217, 217, 217); }
  .about__who-person { margin-top: 12px; }
  /* Prototype: "Roars" at 610, the pitch at 689 — 79px lower, not level. */
  /* The who block opens level with "Team of Roars" at 584, not 8px under it. */
  /* 79 and 296 into the block that opens at 584, so the strip clears the
     pitch by 81 rather than by the grid's row gap. */
  .about__right { grid-column: 3 / 5; }
  .about__pitch { margin-top: 79px; }
  .about__pitch p { max-width: 617px; }
  .about__pitch p {
    font-size: var(--fs-item); font-weight: var(--fw-medium);
    line-height: 34px; letter-spacing: var(--ls-item); color: var(--c-black);
  }
  .about__pitch-mid { color: rgb(71, 71, 71); }
  .about__pitch-low { color: rgb(145, 144, 142); }

  /* 3. services -------------------------------------------------------- */
  /* Measured off the export: section 1360 tall, title box top at 121,
     row container at 306, foot at 1025 running to 1153. */
  .svc { padding-block: 121px 207px; }
  .svc .display { margin-left: var(--rail-width); }
  /* Rows start at 306 in the section. The title box is 121 to 208 (86.8px of
     leading rounds to 87), so the gap is 98. It was 123, which put the whole
     rail 25px low and took every row with it. */
  .svc__list { list-style: none; margin: 98px 0 0; padding: 0; }

  /* ── Services rows ──────────────────────────────────────────────
     Measured off the export, container at 38,306 sized 1364x647:
       row 1  left   0  top   0  h195  OPEN
       row 2  left   0  top 235  h119  closed
       row 3  left 463  top 397  h 96  closed, indented
       row 4  left   1  top 551  h 96  closed
     Two things alternate and one does not, which is why flattening these
     rows twice looked defensible and was wrong both times:
       - the INDENT alternates: rows 1 and 3 start at the rail, 2 and 4 at
         the container edge. A zigzag.
       - the TRACKS follow open/closed, NOT the indent. Every closed row is
         name(465) desc(232) count(166) toggle(36); the open row is
         rail(463) name(241) photo(223) desc(231) clients(167) toggle(37).
     ─────────────────────────────────────────────────────────────── */
  .svc__row {
    position: relative;
    display: grid;
    grid-template-columns: 465px 232px 166px 36px;
    /* The rule's track is ZERO height. The export draws each rule as a
       hairline sitting ON the row's top edge, taking no space; given a real
       auto track it added its own 1-2px to every row, and four rows
       compounded that into the 5px the section was over. */
    grid-template-rows: 0 auto;
    align-items: start;
    justify-content: start;
    /* One rhythm now, where it used to be four hand-measured gaps belonging
       to a four-row band with one row open. Twelve rows need a step, not a
       sequence — and a smaller one, because twelve of the old 40-58px gaps
       is most of a screen spent on whitespace between links. */
    padding-bottom: 26px;
  }
  .svc__row:last-child { padding-bottom: 0; }
  /* An indented row: an empty 463px rail, then the same four tracks.
     fr, not px. The five tracks sum to exactly 1362 — the content box at a
     1440 viewport — so at 1440 fr resolves to the identical widths, and at
     anything narrower it scales instead of hanging off the edge. */
  .svc__row--inner { grid-template-columns: 463fr 465fr 232fr 166fr 36fr; }

  .svc__rule { grid-row: 1; grid-column: 1 / -1; height: 1px; background: var(--c-rule-strong); }
  .svc__row--inner .svc__rule { grid-column: 2 / -1; height: 1.5px; }

  .svc__name {
    grid-row: 2;
    margin: 0;
    padding-top: 27px;
    font-size: var(--fs-item); line-height: 32px;
    font-weight: var(--fw-semi);
    letter-spacing: var(--ls-item); color: var(--c-black);
    transition: color var(--dur-fast) ease;
  }
  .svc__row--inner .svc__name { grid-column: 2; }

  .svc__body {
    grid-row: 2;
    grid-column: 2;
    margin: 0;
    padding-top: 27px;
    /* The track, not 191px. The old cap was measured against four short
       export strings; the real service lines are a sentence each and wrapped
       to four lines inside it, which is what made every row tall. */
    max-width: 232px;
    font-size: var(--fs-body); line-height: 23px;
    font-weight: var(--fw-medium); letter-spacing: -0.06em;
    color: rgb(2, 2, 2);
  }
  .svc__row--inner .svc__body { grid-column: 3; }

  /* The circle the export puts at the row's right edge. It was a toggle; it is
     now the affordance for the row's link and takes no events of its own. */
  .svc__icon {
    grid-row: 2;
    grid-column: 4;
    position: relative;
    justify-self: end;
    margin-top: 27px;
    width: var(--icon-circle); height: var(--icon-circle);
    border-radius: var(--r-pill); background: var(--c-white);
    transition: background var(--dur-fast) ease;
  }
  .svc__row--inner .svc__icon { grid-column: 5; }
  .svc__icon::before, .svc__icon::after {
    content: ''; position: absolute; inset: 50% auto auto 50%;
    translate: -50% -50%; background: var(--c-black);
  }
  .svc__icon::before { width: 13px; height: 1.25px; }
  .svc__icon::after { width: 1.25px; height: 13px; }

  /* The row's link, covering it. Its text is the accessible name and is not
     drawn — the <h3> beside it is what a sighted reader sees, and two copies
     of the same words on screen is what this band used to do wrong. */
  .svc__link {
    position: absolute; inset: 0;
    z-index: 1;
    font-size: 0; color: transparent;
    border-radius: 6px;
  }
  .svc__link:hover ~ .svc__icon,
  .svc__link:focus-visible ~ .svc__icon { background: var(--c-accent); }
  .svc__link:hover ~ .svc__name { color: rgb(90, 90, 88); }

  .svc__foot {
    /* The pill sits at 967, one rail width from the paragraph's 503 — so the
       first track is 464 with no gap, not 356 plus a gap. */
    display: grid; grid-template-columns: 464px 1fr; gap: 0;
    margin-top: 72px; margin-left: var(--rail-width);
    align-items: start;
  }
  .svc__foot .intro { max-width: 356px; }
  .svc__foot :global(a) { justify-self: start; }

  /* 4. projects -------------------------------------------------------- */
  .proj { position: relative; background: var(--c-black); }
  /* Section 1340 tall, the all-line ending at 1195: 145 below it. 68 left
     the section 77 short once the all-line moved up to its measured y. */
  .proj__inner { position: relative; z-index: 1; padding-block: 142px 145px; }
  .proj__head { margin-left: var(--rail-width); }
  .proj__count { margin-top: var(--sp-4); font-size: 40px; font-weight: var(--fw-semi); letter-spacing: var(--ls-display); color: rgb(236, 234, 228); }
  .proj__count span { font-size: 22px; font-weight: var(--fw-medium); }
  /* Label at 732, a 16px white rule at 974. Measured off the export. */
  .proj__legend {
    list-style: none; margin: var(--sp-4) 0 0 229px; padding: 0;
    font-size: var(--fs-body); line-height: 28px;
    letter-spacing: var(--ls-display); color: var(--c-white);
  }
  .proj__legend li { display: grid; grid-template-columns: 242px 16px; align-items: center; }
  .proj__legend i { display: block; height: 1px; background: var(--c-white); }

  .proj__stage { position: relative; margin-top: 77px; min-height: 580px; }
  .proj__slide {
    position: absolute; inset: 0;
    display: grid; grid-template-columns: 120px minmax(0, 1fr);
    gap: var(--sp-4);
    transform-origin: 50% 100%;
    will-change: opacity, transform;
    /* NO transition. The opacity and transform are sampled from the scroll
       position every frame; a transition on top of that lags behind the
       pointer and makes the handover feel elastic instead of tied to the
       wheel. */
  }
  /* The card overlaps. The TEXT does not: the outgoing date and title leave
     the paint and the accessibility tree outright, so two dates are never on
     screen together at any opacity. That collision is why this motion was
     removed once already. */
  .proj__slide[aria-hidden='true'] .proj__date,
  .proj__slide[aria-hidden='true'] .proj__name { visibility: hidden; }
  .proj__slide:first-child { position: relative; }
  .proj__day { font-size: var(--fs-item); line-height: var(--lh-item); font-weight: var(--fw-medium); letter-spacing: var(--ls-body); color: #fbfbfb; }
  .proj__date .rule { margin-block: var(--sp-3); width: 85px; }
  .proj__year { font-size: var(--fs-label); line-height: var(--lh-label); letter-spacing: var(--ls-body); color: var(--c-rule); }
  .proj__card { position: relative; display: block; border-radius: var(--r-card, 12px); overflow: hidden; }
  .proj__ph { display: block; width: 100%; height: 100%; object-fit: cover; background: rgb(30, 30, 30); }
  /* Two ringed faces at the card's top right, as the export draws them. */
  .proj__faces { position: absolute; right: var(--sp-4); top: var(--sp-4); z-index: 3; display: flex; flex-direction: column; gap: 6px; }
  .proj__faces img { width: var(--avatar); height: var(--avatar); border-radius: var(--r-circle); object-fit: cover; box-shadow: var(--shadow-ring); }
  /* rgb(30,30,30) behind the photograph so the layout and the CLS budget hold
     while it loads. 2:1 because that is what build-work-headers writes and
     what both /work/ surfaces show — the export's 1129x579 would have cropped
     a second time in the browser on top of the crop already baked into the
     file. */
  .proj__ph { aspect-ratio: 2 / 1; background: rgb(30, 30, 30); border-radius: 12px; }
  /* Amber dusk. Amber under ink, same order as /work/ — see tokens.css.
     The hero exposure rather than the card one, because what sits on this
     photograph is a name along the bottom edge and two ringed faces in the
     top corner, not a paragraph across the middle. The ink runs bottom-up and
     puts the contrast under the name; holding the whole 1129px picture back
     to 0.62 as well would be paying for contrast twice. */
  .proj__ph { filter: brightness(var(--dusk-exposure-hero)); }
  .proj__card::before,
  .proj__card::after {
    content: ''; position: absolute; inset: 0; pointer-events: none;
  }
  .proj__card::before { z-index: 1; background: var(--dusk-amber); }
  .proj__card::after { z-index: 2; background: var(--dusk-ink-up); }
  .proj__name {
    position: absolute; left: 50%; bottom: 62px; z-index: 3;
    line-height: 42px;
    font-size: 36px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-display); color: var(--c-white);
  }
  /* The "all line" is a 430x47 block at 503,1148: two star marks at 0 and 23,
     then the pill at 226. The marks belong beside the pill, not adrift in the
     rail — they were 226px too far left and sitting below it. */
  .proj__foot { display: grid; grid-template-columns: 226px auto; margin-top: 30px; margin-left: var(--rail-width); }
  .proj__foot :global(a) { grid-column: 2; justify-self: start; }
  .proj__stars, .ind__stars { display: flex; gap: 6px; align-self: start; margin-top: 16px; }

  /* 5. people say ------------------------------------------------------ */
  .say { padding-block: 147px 95px; }
  .say .display, .say__score { margin-left: var(--rail-width); }
  .say__score { margin-top: var(--sp-4); font-size: var(--fs-stat); line-height: var(--lh-stat); font-weight: var(--fw-semi); letter-spacing: var(--ls-display); color: var(--c-ink-heading); }

  /* The cascade. Seven tracks so the four items land on the prototype's
     uneven column rhythm (292 / 57 / 339 / 7 / 435 / 29 / 206 of 1365); each
     item then drops by its own offset. Proportional, so it holds off 1440. */
  /* The cascade is an authored 525px band (container 39,329 to 854 in the
     export), not a content-driven one — the four cards carry deliberate
     vertical offsets, so letting the tallest set the height put the review
     bar below it 37px low. min-height, not height: the export's container
     clips at 525 and its mock quotes fit; the real testimonials are longer,
     and cutting a client's words off mid-sentence to save 37px is not a
     trade worth making. Where the band has to grow, it grows. */
  .say__list {
    list-style: none; margin: 27px 0 0; padding: 0;
    display: grid;
    grid-template-columns: 292fr 57fr 339fr 7fr 435fr 29fr 206fr;
    align-items: start;
    min-height: 525px;
  }
  .say__item {
    grid-row: 1;
    grid-column: var(--col);
    margin-top: var(--top);
  }
  .say__avatar { display: block; width: var(--av); height: var(--av); border-radius: var(--r-circle); object-fit: cover; background: rgb(217, 217, 217); }
  .say__name { margin: 15px 0 0; font-size: var(--lb); font-weight: var(--fw-semi); line-height: 19px; letter-spacing: var(--ls-body); color: rgb(31, 31, 31); }
  .say__role { margin: 0; font-size: var(--lb); font-weight: var(--fw-medium); line-height: 19px; letter-spacing: var(--ls-body); color: var(--c-ink-label); }
  .say__rule { height: 1.5px; margin-top: 19px; background: var(--c-rule-strong); }
  .say__stars { display: flex; gap: 4px; margin-top: 22px; }
  .say__stars img { width: var(--st); height: var(--st); }
  .say__quote {
    margin: 28px 0 0;
    font-size: var(--qt); font-weight: var(--fw-medium);
    line-height: var(--ld); letter-spacing: var(--ls-item); color: var(--c-black);
  }

  /* 386 / 737 / 966 in the prototype, which is its own rhythm rather than
     the inner column. */
  /* Measured: the nav block at 386,880 and the stack block at 737,866, so
     they are 351 apart and NOT baseline-aligned — the nav sits 14 lower.
     Within the stack block: avatars 0-138, meta at 150, pill label at 991
     page-absolute, so the pill starts 229 in and the meta track is 79.
     (I briefly set that track to 314 off a `left:464px` found in the MENU
     overlay's contact panel, not this section, and it threw the pill 235px
     right. Grep the right block.) */
  .say__foot { display: grid; grid-template-columns: 351px 150px 79px auto; align-items: start; margin-top: 12px; margin-left: 347px; }
  .say__foot :global(a) { justify-self: start; }
  /* 190px band: dot at 0, "Prev." at 18, "Next" at 128, dot at 182. */
  .say__nav { display: grid; grid-template-columns: 8px 102px 42px 8px; gap: 0 10px; width: 190px; align-items: center; margin-top: 14px; font-size: var(--fs-body); line-height: var(--lh-body); font-weight: var(--fw-medium); letter-spacing: var(--ls-display); }
  .say__prev { color: rgba(0, 0, 0, 0.4); }
  .say__next { color: rgb(8, 8, 8); }
  .say__dot { width: 8px; height: 8px; border-radius: var(--r-circle); background: rgba(0, 0, 0, 0.4); }
  .say__dot--on { background: rgb(8, 8, 8); }
  /* Overlapping stack: 42px discs at a 32px pitch, each ringed in the ground
     colour so the overlap reads as depth rather than a smudge. */
  .say__stack { display: flex; }
  .say__stack > * {
    width: var(--avatar); height: var(--avatar);
    border-radius: var(--r-circle);
    background: rgb(217, 217, 217);
    box-shadow: var(--shadow-ring);
  }
  .say__stack > * + * { margin-left: var(--avatar-overlap); }
  .say__count {
    display: flex; align-items: center; justify-content: center;
    background: rgb(20, 20, 20);
    font-size: 15px; font-weight: var(--fw-semi);
    letter-spacing: var(--ls-item); color: var(--c-white);
  }
  .say__meta { margin: 0; font-size: 13px; font-weight: var(--fw-medium); line-height: 18px; letter-spacing: var(--ls-display); color: rgb(71, 71, 71); }

  /* 6. industries ------------------------------------------------------ */
  .ind { padding-block: 120px; position: relative; }
  .ind .display, .ind__intro, .ind__grid, .ind__foot {
    margin-left: var(--rail-width);
  }
  /* The label pair belongs to the left rail, not the inner column. */
  .ind__rail { position: absolute; left: 3px; top: 319px; }
  .ind__label { margin: 0; font-size: var(--fs-label); line-height: var(--lh-label); font-weight: var(--fw-medium); letter-spacing: var(--ls-display); color: var(--c-black); }
  .ind__sub { margin: 0; font-size: var(--fs-label); line-height: var(--lh-label); font-weight: var(--fw-regular); letter-spacing: var(--ls-display); color: var(--c-rule); }
  .ind__intro { margin-top: var(--sp-4); max-width: 756px; }
  .ind__grid { list-style: none; margin: 202px 0 0 var(--rail-width); padding: 0; display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 107px 20px; }

  /* Horizontal: ring circle on the left, name and cta to its right, rule
     above. Square boxes with inset rings were the earlier reading and are
     not what the prototype does. */
  .ind__card {
    display: grid;
    grid-template-columns: 64px minmax(0, 1fr);
    align-items: center;
    gap: var(--sp-3);
    padding-top: 28px;
    border-top: 1px solid var(--c-rule-strong);
  }
  .ind__letter {
    display: flex; align-items: center; justify-content: center;
    width: 64px; height: 64px;
    border-radius: var(--r-circle);
    box-shadow: inset 0 0 0 1.5px rgba(20, 20, 20, 0.35);
    font-size: var(--fs-intro); font-weight: var(--fw-semi); line-height: 1;
    letter-spacing: var(--ls-display); color: var(--c-ink-heading);
    transition: box-shadow var(--dur-fast) ease, background var(--dur-fast) ease;
  }
  .ind__card:hover .ind__letter { box-shadow: inset 0 0 0 1.5px var(--c-accent); background: rgba(255, 212, 0, 0.14); }
  .ind__text { display: flex; flex-direction: column; gap: 2px; }
  .ind__name { font-size: var(--fs-intro); font-weight: var(--fw-semi); line-height: 28px; letter-spacing: var(--ls-display); color: var(--c-ink-heading); }
  .ind__cta { font-size: var(--fs-micro); font-weight: var(--fw-medium); line-height: 19px; letter-spacing: var(--ls-item); color: var(--c-rule); }
  .ind__foot { display: grid; grid-template-columns: 232px auto; margin-top: var(--sp-7); }
  .ind__foot :global(a) { grid-column: 2; justify-self: start; }

  /* 7. insights -------------------------------------------------------- */
  .ins { padding-block: 120px 79px; }
  .ins .display, .ins__head, .ins__foot { margin-left: var(--rail-width); }
  .ins__head { display: grid; grid-template-columns: 232px minmax(0, 524px); gap: 0; margin-top: 92px; align-items: start; }
  .ins__avatar { display: block; width: var(--avatar); height: var(--avatar); border-radius: var(--r-circle); object-fit: cover; background: rgb(217, 217, 217); }
  .ins__name { margin: 18px 0 0; font-size: var(--fs-label); font-weight: var(--fw-semi); line-height: 20px; letter-spacing: var(--ls-display); color: rgb(22, 22, 22); }
  .ins__role { margin: 0; font-size: var(--fs-label); font-weight: var(--fw-medium); line-height: 20px; letter-spacing: var(--ls-display); color: rgb(145, 144, 142); }
  .ins__intro { margin: 0; }

  /* Three cards across the FULL width, gutter to gutter, not inside the
     inner column: the first card's month sits at x=94 in the prototype. */
  .ins__grid { list-style: none; margin: 126px 0 0; padding: 0; display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 29px; }
  .ins__card {
    display: grid;
    grid-template-columns: 232px minmax(0, 1fr);
    align-items: start;
    row-gap: 0;
  }
  /* A 138px disc, offset into the second column, matching the services row. */
  .ins__ph { grid-column: 2; width: 138px; height: 138px; border-radius: var(--r-circle); object-fit: cover; background: rgb(217, 217, 217); }
  .ins__rule { grid-column: 1 / -1; height: 1px; margin-top: 36px; background: var(--c-rule-strong); }
  .ins__day { grid-column: 1; grid-row: 3; justify-self: start; margin-top: 13px; font-size: var(--fs-stat); font-weight: var(--fw-semi); line-height: 43px; letter-spacing: var(--ls-display); color: var(--c-ink-heading); }
  .ins__month { grid-column: 1; grid-row: 3; justify-self: start; margin: 13px 0 0 55px; font-size: var(--fs-body); font-weight: var(--fw-medium); line-height: 43px; letter-spacing: var(--ls-display); color: var(--c-ink-heading); }
  .ins__title { grid-column: 2; grid-row: 3; max-width: 195px; margin-top: 25px; font-size: var(--fs-body); font-weight: var(--fw-medium); line-height: 22px; letter-spacing: var(--ls-body); color: rgb(20, 20, 20); }
  .ins__card:hover .ins__title { color: rgb(90, 90, 90); }
  .ins__foot { display: grid; grid-template-columns: 232px auto; align-items: center; margin-top: 115px; }
  .ins__foot :global(a) { justify-self: start; }
  .ins__stars { display: flex; gap: 5px; }

  /* 8. faq ------------------------------------------------------------- */
  .faq { padding-block: 120px 135px; position: relative; }
  .faq .display, .faq__foot { margin-left: var(--rail-width); }
  .faq :global(.acc) { margin-top: var(--sp-7); margin-left: var(--rail-width); }
  /* Founder block in the left rail. */
  .faq__rail { position: absolute; left: 3px; top: 308px; width: 195px; }
  .faq__avatar { display: block; width: var(--avatar); height: var(--avatar); border-radius: var(--r-circle); object-fit: cover; background: rgb(217, 217, 217); }
  .faq__who { margin: 10px 0 0; font-size: var(--fs-micro); font-weight: var(--fw-medium); line-height: 19px; letter-spacing: var(--ls-body); color: var(--c-black); }
  .faq__role { margin: 0; font-size: var(--fs-micro); font-weight: var(--fw-regular); line-height: 19px; letter-spacing: var(--ls-body); color: var(--c-rule); }
  .faq__intro { margin: var(--sp-4) 0 0; font-size: var(--fs-body); font-weight: var(--fw-medium); line-height: 22px; letter-spacing: var(--ls-body); color: rgb(20, 20, 20); }
  .faq__foot { margin-top: var(--sp-6); display: grid; grid-template-columns: 232px auto; align-items: center; }
  .faq__foot :global(a) { justify-self: start; }
  .faq__ask { max-width: 200px; margin: 0; font-size: var(--fs-body); font-weight: var(--fw-medium); line-height: 22px; letter-spacing: var(--ls-body); color: rgb(20, 20, 20); }

  /* responsive --------------------------------------------------------- */
  @media (max-width: 1040px) {
    .hero__inner { grid-template-columns: 1fr; padding-block: 180px 80px; row-gap: var(--sp-6); }
    /* minmax(0, 1fr): see the ≤760 block. `1fr` floors at the widest child's
       min-content, which the logo marquee sets to 914px. */
    .about { grid-template-columns: minmax(0, 1fr); }
    .about > * { min-width: 0; }
    .about__stats, .about__est, .about__who, .about__right { grid-column: 1 / -1; }
    /* Every 464px rail indent, from the declarations that actually set one.
       The earlier list was written from memory and named .ins__who and
       .faq__who, which never had a margin, while missing .ins__head — which
       did, and which therefore kept a 464px indent inside a 350px page and
       collapsed to zero width. */
    .svc .display, .svc__foot,
    .proj__head, .proj__foot,
    .say .display, .say__score, .say__foot,
    .ind .display, .ind__intro, .ind__grid, .ind__foot,
    .ins .display, .ins__head, .ins__foot,
    .faq .display, .faq__foot { margin-left: 0; }
    .faq :global(.acc) { margin-left: 0; }
    .svc__grid { grid-template-columns: 1fr 1fr; }
    .svc__foot { grid-template-columns: 1fr; }
    .say__list { grid-template-columns: 1fr 1fr; min-height: 0; gap: 40px 24px; }
    /* Each card names its own column and its own top offset, which is how the
       prototype staggers them across a seven-track row. Narrowing the track
       list does not clear those: an item still asking for column 5 makes the
       grid grow an implicit column to hold it. They flow in order instead. */
    .say__item { grid-column: auto; grid-row: auto; margin-top: 0; }
    .ind__grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .ins__grid { grid-template-columns: 1fr; }

    /*
     * Unstacking the fixed tracks starts HERE, not at 760.
     *
     * The desktop layout is a 1362 canvas, so the section feet and heads are
     * two-column grids with a 232px rail and an absolute-positioned side rail
     * at a fixed top offset. Those do not fit a tablet either. Leaving them
     * until 760 left an 800px-wide phone in landscape, and every tablet,
     * rendering the desktop geometry into a box too small for it — which is
     * the same clipping bug one breakpoint up.
     *
     * The ≤760 block below is then only what is genuinely phone-specific:
     * type scale, single columns, and the two blocks whose absolute
     * placements are tied to an 84px numeral.
     */
    .proj__head, .proj__foot,
    .say__foot, .ind__foot, .ins__head, .ins__foot, .faq__foot {
      display: flex; flex-direction: column; align-items: stretch;
      grid-template-columns: none;
    }
    /* stretch, not flex-start: a flex item aligned to the start shrinks to
       max-content, which left the project legend and the section intros
       sized to their longest line and hanging off the page. The pills are
       the exception — they are a fixed 204 and should not run the width. */
    .proj__head > a, .proj__foot > a, .say__foot > a,
    .ind__foot > a, .ins__foot > a, .faq__foot > a,
    .proj__head :global(.btn), .proj__foot :global(.btn), .say__foot :global(.btn),
    .ind__foot :global(.btn), .ins__foot :global(.btn), .faq__foot :global(.btn) {
      align-self: flex-start;
    }
    .proj__head, .ins__head { gap: 20px; }
    .proj__foot, .say__foot, .ind__foot, .ins__foot, .faq__foot { gap: 18px; }
    /* Side rails placed at a fixed top offset against a fixed section height. */
    .ind__rail, .faq__rail { position: static; width: auto; margin-top: var(--sp-4); }
    /* Widths measured off the 1362 box.
       100%, not none: these sit in flex columns aligned to the start, so
       `none` lets them size to max-content and run past the page edge. */
    .ind__intro, .ins__intro, .faq__intro, .ins__title, .faq__ask, .say__meta,
    .svc__foot .intro { max-width: 100%; }
    /* The service rows are four and five fr tracks on the 1362 canvas. fr does
       not wrap, so at tablet width the rule and the circle were painting past
       the section edge. */
    .svc__row, .svc__row--inner {
      display: flex; flex-direction: column; align-items: stretch; gap: 10px;
      grid-template-columns: none;
    }
    /* `justify-self` is a grid property and does nothing in a flex column, so
       the circle stretched to the full row width and read as a bar. */
    .svc__icon { align-self: flex-end; margin-top: 4px; }
    /* 229px of left margin measured off the 1362 canvas. */
    .proj__legend { margin-left: 0; }
    .proj__legend li { grid-template-columns: minmax(0, 1fr) 16px; }
    .say__nav { width: auto; }
    /* The + and the % are absolutely placed at 152,17 and 110,19 against an
       84px numeral. Below the design grid the numeral clamps down — 123px
       wide at 768 instead of 200 — while the offsets do not, so both signs
       landed past the figure and on top of the note beside it. Back to being
       the next character along, which is what they read as anyway. */
    .about__stat-num { position: static; }
    .about__plus, .about__pct {
      position: static; margin-left: 6px; line-height: normal;
      vertical-align: baseline;
    }

    /* Section rhythm. Every vertical gap on this page was measured off the
       1440 canvas, where a section is a tenth of the page. Kept as-is they
       are a fifth of a small screen each, and the page reads as a run of
       empty panels with a paragraph in the middle of each. The spec's mobile
       sections are 56-64px of padding and 32-40px between blocks. */
    .about { padding-block: 56px; }
    .svc { padding-block: 56px 72px; }
    .svc__list { margin-top: 40px; }
    .svc__foot { margin-top: 48px; }
    .proj__inner { padding-block: 56px 64px; }
    .proj__stage { margin-top: 40px; }
    .proj__foot { margin-top: 40px; }
    .say { padding-block: 56px 64px; }
    .say__list { margin-top: 32px; }
    .ind { padding-block: 56px 64px; }
    .ind__grid { margin-top: 44px; gap: 36px 20px; }
    .ins { padding-block: 56px 64px; }
    .faq { padding-block: 56px 72px; }
    .faq :global(.acc) { margin-top: 36px; }
    .about__pitch { margin-top: 40px; }
  }

  /*
   * ≤760: the documented mobile stack.
   *
   * One DOM, reflowed. The prototype ships a second markup tree and toggles
   * the two with display:none; that doubles the document and lets the two
   * versions drift apart, which is how a fix lands on desktop and not on a
   * phone. Everything below re-flows the same elements.
   *
   * Type sizes and stacking come from design/specs/Main.md, "Mobile layout
   * (≤760px)", sections M1-M9. Where the shipped page has moved past the
   * prototype — the hero is the yellow "Ship product that scales" lockup, not
   * the prototype's dark wordmark — the shipped content wins and only the
   * scale comes from the spec.
   *
   * THE RECURRING BUG this fixes: the desktop layout is a 1362 canvas, so
   * almost every block carries a fixed or fr-based grid. `fr` tracks do not
   * collapse — 435fr 29fr 435fr stays 3 columns at 390px and simply overflows
   * — and the sections clip with overflow:hidden, so the content did not
   * scroll sideways, it vanished. 105 elements were painting off-screen.
   */
  @media (max-width: 760px) {
    .display { font-size: 56px; line-height: 56px; }
    .intro { font-size: 19px; line-height: 28px; }


    /* hero ------------------------------------------------------------- */
    .hero { min-height: 0; }
    .hero__inner { padding-block: 128px 56px; }
    /* The 130px line-height was fixed while the size clamped down to 56,
       which left two thirds of a line of air between every line. */
    .hero__h1 { font-size: clamp(44px, 13vw, 72px); line-height: 1.06; letter-spacing: -0.045em; }
    /* 360px of reserved width does not fit a 350px content box. The word
       swap shifts the line on a phone; that is better than a scrollbar. */
    .hero__rotate { min-width: 0; }
    .hero__lede { margin-top: 32px; font-size: 17px; line-height: 26px; }
    .hero__cta { margin-top: 24px; }
    .hero__rule { margin-top: 44px; }
    /* Four labels across 350px is four stacked characters per column. */
    .hero__labels {
      display: grid; grid-template-columns: 1fr 1fr; gap: 14px 18px;
      margin-top: 20px; font-size: 11px;
    }

    /* about ------------------------------------------------------------ */
    /* minmax(0, 1fr), not 1fr. A bare `1fr` is minmax(auto, 1fr), so the
       track cannot shrink below its widest child's min-content — and the
       logo marquee is `width: max-content`, which made that 914px. The whole
       About column was sized by the marquee and clipped at the section edge.
       min-width on the children is the same guard one level down. */
    .about { grid-template-columns: minmax(0, 1fr); row-gap: 48px; padding-block: 56px; }
    .about > * { min-width: 0; }
    .about__stars { position: static; margin-bottom: 8px; }
    .about__stats, .about__stat-pair {
      display: flex; flex-direction: column; gap: 36px;
      grid-template-columns: none; grid-template-rows: none; min-height: 0;
    }
    .about__est, .about__who, .about__team, .about__right { grid-column: 1 / -1; }
    .about__stat-row, .about__est-row {
      display: flex; flex-direction: column; align-items: flex-start; gap: 6px;
      margin-top: 10px;
    }
    /* The + and % are absolutely placed against an 84px numeral on desktop.
       At 72px those offsets land in the middle of the digits, so they go
       back to being what they read as: the next character along. */
    .about__stat-num { font-size: 72px; line-height: 72px; }
    .about__plus, .about__pct {
      position: static; margin-left: 6px; line-height: normal;
    }
    .about__plus { font-size: 32px; }
    .about__pct { font-size: 26px; }
    .about__stat-name { font-size: 20px; }
    .about__stat-note, .about__est-note { max-width: none; font-size: 15px; line-height: 21px; }
    .about__est-num { line-height: 72px; }
    .about__est-side { margin-top: 0; }
    .about__team-name { font-size: 22px; }
    /* The cluster is four absolutely-placed faces in a 145x143 box, which on
       a phone reads as scattered grey circles. The spec's mobile row is the
       same faces shingled behind the counter. */
    .about__cluster { display: flex; align-items: center; width: auto; height: auto; margin-top: 24px; }
    .about__face, .about__team-count { position: static; margin-left: -10px; box-shadow: 0 0 0 3px var(--c-white); }
    .about__team-count { order: -1; margin-left: 0; box-shadow: none; }
    /* Placed at 10,8 on a 43px leading against the desktop disc. In a flex
       row that reads as a numeral falling out of its circle. */
    .about__team-count > span {
      position: static; display: flex; align-items: center; justify-content: center;
      width: 100%; height: 100%; line-height: 1;
    }
    .about__who-face { margin-top: 20px; }
    .about__who-name, .about__who-sub { font-size: 22px; line-height: 26px; }
    .about__pitch p { max-width: none; font-size: 19px; line-height: 28px; }
    /* The fade edges assume the strip is inset from the page. At full bleed
       they would sit over the marks themselves. */
    .about__logos { width: 100%; margin: 44px 0 0; }
    .about__logos::before, .about__logos::after { width: 24px; }

    /* services --------------------------------------------------------- */
    .svc__grid { grid-template-columns: 1fr; gap: var(--sp-4); }
    .svc__name { font-size: 22px; line-height: 28px; }
    .svc__body { font-size: 15px; line-height: 22px; }
    .svc__foot { row-gap: var(--sp-4); }

    /* projects --------------------------------------------------------- */
    .proj__slide { grid-template-columns: 1fr; }
    /* Centred on a 50% offset, which at 350px puts the name off the card. */
    .proj__name {
      position: static; transform: none; margin-top: 12px;
      font-size: 28px; line-height: 34px;
    }
    .proj__faces { position: static; flex-direction: row; margin-top: 12px; }
    /* Nothing sits on the photograph at this width — the name and the faces
       drop below it — so the ink has no copy to carry, and both layers cover
       the card, which down here is taller than the picture. Off; the exposure
       is already the one the photograph gets when it is the subject. */
    .proj__card::before, .proj__card::after { display: none; }
    /* The slides are absolute so they can cross-fade over one another, which
       means the stage carries the height — 580px, measured on the two-column
       desktop card. Stacked to one column the card is taller than that and
       ran out of the section.
       Same cell, still stacked, but as a grid so the stage sizes itself to
       the tallest slide. The island keeps working untouched: it only sets
       opacity, transform and visibility. */
    .proj__stage { min-height: 0; display: grid; }
    .proj__slide { position: relative; inset: auto; grid-area: 1 / 1; }

    /* people say ------------------------------------------------------- */
    .say__list { grid-template-columns: 1fr; }
    .say__item .say__quote { font-size: 18px !important; line-height: 26px !important; }

    /* industries ------------------------------------------------------- */
    .ind__grid { grid-template-columns: 1fr; }
    .ind__foot { margin-top: 48px; }

    /* insights --------------------------------------------------------- */
    .ins__head, .ins__foot { margin-top: 40px; }
    .ins__grid { grid-template-columns: 1fr; margin-top: 48px; gap: 40px; }
    /* The card puts the day and the month in ONE cell, the month pushed
       across by a 55px margin, and the title in the second column. Collapse
       the columns and all three land on top of each other. Day and month get
       a track each; the disc, the rule and the title span both. */
    .ins__card { grid-template-columns: auto minmax(0, 1fr); }
    .ins__ph, .ins__rule { grid-column: 1 / -1; }
    .ins__day { grid-column: 1; margin-top: 10px; }
    .ins__month { grid-column: 2; margin: 10px 0 0 8px; justify-self: start; }
    .ins__title { grid-column: 1 / -1; grid-row: 4; margin-top: 4px; }

    /* faq -------------------------------------------------------------- */
    .faq__rail { margin-top: var(--sp-4); }
  }
</style>
